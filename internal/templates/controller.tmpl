package modules

import (
	"encoding/json"
	"net/http"
)

type {{.FeatureName}}Controller struct {
	service {{.FeatureName}}Service
}

func New{{.FeatureName}}Controller(service {{.FeatureName}}Service) *{{.FeatureName}}Controller {
	return &{{.FeatureName}}Controller{
		service: service,
	}
}

func (c *{{.FeatureName}}Controller) HandleGet(w http.ResponseWriter, r *http.Request) {
	result := c.service.DoBusinessLogic()
	json.NewEncoder(w).Encode(map[string]string{"message": result})
}
